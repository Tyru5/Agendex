{
  "buildCommand": "bunx convex deploy --convex-dir packages/ee/convex --cmd 'bun run build:cloud'",
  "outputDirectory": "packages/ee/dist",
  "installCommand": "bun install",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
