name: Release Desktop

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Release version (for example 1.0.0 or desktop-v1.0.0)'
        required: true
        type: string
  push:
    tags:
      - 'desktop-v*.*.*'

permissions:
  contents: write

concurrency:
  group: desktop-release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  preflight:
    name: Preflight
    runs-on: ubuntu-latest
    timeout-minutes: 15
    outputs:
      version: ${{ steps.release_meta.outputs.version }}
      tag: ${{ steps.release_meta.outputs.tag }}
      release_name: ${{ steps.release_meta.outputs.release_name }}
      is_prerelease: ${{ steps.release_meta.outputs.is_prerelease }}
      make_latest: ${{ steps.release_meta.outputs.make_latest }}
      ref: ${{ github.sha }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - id: release_meta
        name: Resolve release metadata
        env:
          RAW_VERSION: ${{ github.event.inputs.version || github.ref_name }}
        run: node scripts/prepare-desktop-release.mjs "$RAW_VERSION" --github-output

      - name: Check formatting and lint
        run: bun run check

      - name: Run desktop-related tests
        run: bun test packages/desktop packages/shared/src/convex-url.test.ts packages/shared/src/desktop-auth-callback.test.ts packages/ee/src/lib/desktop.test.ts packages/ee/src/lib/desktop-auth-flow.test.ts packages/ee/src/lib/cli-auth-callback.test.ts packages/ee/src/lib/cloud-query-mode.test.ts

  release:
    name: Build and publish macOS
    needs: preflight
    runs-on: macos-latest
    # Generous limit: Apple notarization can stall for an hour+ during queue backlogs.
    timeout-minutes: 120
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.preflight.outputs.ref }}

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Align desktop package version
        run: node scripts/prepare-desktop-release.mjs "${{ needs.preflight.outputs.version }}" --write

      - name: Build desktop app
        run: bun run desktop:build

      - name: Prepare notarization key
        shell: bash
        env:
          APPLE_API_KEY_B64: ${{ secrets.APPLE_API_KEY }}
          APPLE_API_KEY_ID: ${{ secrets.APPLE_API_KEY_ID }}
        run: |
          set -euo pipefail
          if [[ -z "${APPLE_API_KEY_B64:-}" ]]; then
            echo "APPLE_API_KEY is not set; skipping App Store Connect API key decode."
            exit 0
          fi

          key_id="${APPLE_API_KEY_ID:-AuthKey}"
          key_path="$RUNNER_TEMP/AuthKey_${key_id}.p8"
          if printf '%s' "$APPLE_API_KEY_B64" | base64 --decode >"$key_path" 2>/dev/null; then
            :
          else
            printf '%s' "$APPLE_API_KEY_B64" | base64 -D >"$key_path"
          fi
          echo "APPLE_API_KEY=$key_path" >>"$GITHUB_ENV"

      - name: Verify macOS signing secrets
        shell: bash
        env:
          CSC_LINK: ${{ secrets.CSC_LINK }}
          CSC_KEY_PASSWORD: ${{ secrets.CSC_KEY_PASSWORD }}
          APPLE_API_KEY: ${{ secrets.APPLE_API_KEY }}
          APPLE_API_KEY_ID: ${{ secrets.APPLE_API_KEY_ID }}
          APPLE_API_ISSUER: ${{ secrets.APPLE_API_ISSUER }}
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
        run: |
          set -euo pipefail
          missing=()
          [[ -n "${CSC_LINK:-}" ]] || missing+=(CSC_LINK)
          [[ -n "${CSC_KEY_PASSWORD:-}" ]] || missing+=(CSC_KEY_PASSWORD)

          has_api_key=false
          if [[ -n "${APPLE_API_KEY:-}" && -n "${APPLE_API_KEY_ID:-}" && -n "${APPLE_API_ISSUER:-}" ]]; then
            has_api_key=true
          fi

          has_apple_id=false
          if [[ -n "${APPLE_ID:-}" && -n "${APPLE_APP_SPECIFIC_PASSWORD:-}" ]]; then
            has_apple_id=true
          fi

          if [[ "$has_api_key" != "true" && "$has_apple_id" != "true" ]]; then
            missing+=(APPLE_API_KEY/APPLE_API_KEY_ID/APPLE_API_ISSUER)
          fi

          if (( ${#missing[@]} > 0 )); then
            printf '::error::Missing macOS signing/notarization secrets: %s\n' "${missing[*]}"
            exit 1
          fi

      - name: Package macOS desktop artifact
        env:
          CSC_LINK: ${{ secrets.CSC_LINK }}
          CSC_KEY_PASSWORD: ${{ secrets.CSC_KEY_PASSWORD }}
          APPLE_API_KEY_ID: ${{ secrets.APPLE_API_KEY_ID }}
          APPLE_API_ISSUER: ${{ secrets.APPLE_API_ISSUER }}
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        run: bun run --cwd packages/desktop dist -- --mac --universal

      # electron-builder staples the notarization ticket to the .app inside the
      # DMG, but not to the DMG container itself. Without a stapled DMG,
      # Gatekeeper can't assess the disk image offline and some download paths
      # surface a bogus "Agendex is damaged" dialog. Staple the DMG so
      # `spctl -a -t open --context context:primary-signature <dmg>` passes.
      - name: Staple notarization ticket to DMG
        shell: bash
        run: |
          set -euo pipefail
          shopt -s nullglob
          for dmg in packages/desktop/release/*.dmg; do
            echo "Stapling $dmg"
            xcrun stapler staple "$dmg"
            xcrun stapler validate "$dmg"
          done

      - name: Collect release assets
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p release-publish
          shopt -s nullglob

          for pattern in \
            "packages/desktop/release/*.dmg" \
            "packages/desktop/release/*.zip" \
            "packages/desktop/release/*.blockmap" \
            "packages/desktop/release/*.yml"; do
            for file in $pattern; do
              case "$(basename "$file")" in
                builder-debug.yml) continue ;;
              esac
              cp "$file" release-publish/
            done
          done

          ls -lah release-publish

      - name: Publish GitHub release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ needs.preflight.outputs.tag }}
          name: ${{ needs.preflight.outputs.release_name }}
          files: release-publish/*
          draft: false
          prerelease: ${{ needs.preflight.outputs.is_prerelease == 'true' }}
          make_latest: ${{ needs.preflight.outputs.make_latest == 'true' }}
          generate_release_notes: true
          body: |
            Agendex Desktop for macOS (universal). Windows builds will follow in a later release.

            ## Install
            Download the `.dmg` for the standard install flow, or the `.zip` if you prefer to drag the app into Applications yourself.

            ## Verify (optional)
            After opening the app:
            ```bash
            codesign --verify --deep --strict --verbose=2 "/Applications/Agendex.app"
            spctl -a -vv "/Applications/Agendex.app"
            ```

      # Keep /download pointing at the just-published stable assets. Applied on
      # top of origin/main so a release from an older tag still lands a clean
      # version-only commit (package.json stays at the 0.0.0 dev baseline).
      - name: Bump download page on main
        if: needs.preflight.outputs.make_latest == 'true'
        shell: bash
        env:
          VERSION: ${{ needs.preflight.outputs.version }}
        run: |
          set -euo pipefail

          git fetch origin main
          git checkout -B desktop-download-version origin/main

          node scripts/prepare-desktop-release.mjs "$VERSION" --write
          # Packaging version is ephemeral; only the marketing constant is committed.
          git checkout -- packages/desktop/package.json

          if git diff --quiet -- packages/web/src/client/components/DownloadPage.tsx; then
            echo "Download page already at v${VERSION}"
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add packages/web/src/client/components/DownloadPage.tsx
          git commit -m "chore(web): bump desktop download version to ${VERSION}"
          git push origin HEAD:main
