{
  "name": "@agendex/desktop",
  "version": "0.0.0",
  "private": true,
  "description": "Agendex desktop app (Electron)",
  "main": "./out/main/index.js",
  "scripts": {
    "dev": "concurrently -k -n vite,electron -c green,cyan \"bun run --cwd ../ee dev:client\" \"cross-env AGENDEX_RENDERER_URL=http://app.agendex.localhost:5174/dashboard electron-vite dev --watch\"",
    "build": "bun run --cwd ../ee build && electron-vite build",
    "dist": "bun run --cwd ../ee build && electron-vite build && electron-builder",
    "dist:mac": "bun run --cwd ../ee build && electron-vite build && electron-builder --mac",
    "dist:win": "bun run --cwd ../ee build && electron-vite build && electron-builder --win"
  },
  "dependencies": {
    "@agendex/app": "workspace:*",
    "@agendex/ee": "workspace:*",
    "@electron-toolkit/preload": "^3.0.2",
    "@electron-toolkit/utils": "^4.0.0"
  },
  "devDependencies": {
    "concurrently": "^9.0.3",
    "cross-env": "^10.1.0",
    "electron": "^42.5.0",
    "electron-builder": "^26.15.3",
    "electron-vite": "^5.0.0"
  }
}
